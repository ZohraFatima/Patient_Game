/*using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using System.Reflection;
using UnityEngine;
using Debug = UnityEngine.Debug;
public class program : MonoBehaviour
{
    // Start is called before the first frame update
    public slowMovement sm;
    public fastMovement fm;
    public repetitions r;
    public circle_movement c;
    bool check = true;
    public program()
    {
        
            Debug.Log("program");
            slowMovement sm = new slowMovement();
            fastMovement fm = new fastMovement();
            repetitions r = new repetitions();
        Debug.Log("Here");//($"sm: {sm}, fm: {fm}, r: {r}");
            c = new circle_movement(sm, fm, r);
            check = false;
        
       
    }
    
}*/
